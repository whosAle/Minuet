Alejandro Belgrave

*********************************************

Extract the TFiles and Mfile into minuet directory. Program will not work if this step is not done.

Run the python file name 'mozart' contained within the directory of that name. Python 3.

*********************************************

The program generates a minuet from random sound samples in a bank.